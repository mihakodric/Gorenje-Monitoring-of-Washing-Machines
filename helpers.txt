Vpis v git:
git config --global user.name "Nina Pregrad"
git config --global user.email "pregrad.nina@gmail.com"


Izpis iz git-a
git config --global --unset user.name
git config --global --unset user.email

Instalirani extensi:

copilot
C/C++ - vsi trije extensioni ---> potem pa inštaliraš še MSYS2 za windows (preko spleta)
namestitev Arduino CLI (preko spleta, .msi) + namestitev esp32 plošče
